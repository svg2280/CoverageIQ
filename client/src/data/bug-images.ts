// Auto-generated from bug-images.json — bundled into the JS so it works
// inside any iframe/proxy where /bug-images.json may 404.
export type BugImage = { url: string; caption?: string; license?: string };
import data from "../../public/bug-images.json";
export const BUG_IMAGES = data as Record<string, BugImage>;
